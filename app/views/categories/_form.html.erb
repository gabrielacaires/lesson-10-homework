<%= form_for @category do |c| %>
  <p>
    <% if @category.errors.any? %>
      <h3><%= pluralize(@category.errors.count, "error") %> prohibited this character from being saved:</h3>
      <ul>
        <% @category.errors.full_messages.each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    <% end %>
  </p>

  <p>
    <%= c.label :name %>
    <%= c.text_field :name %>
  </p>

  <p>
    <%= c.submit %>
  </p>
<% end %>